# FIXED: Book Now button using CALLBACK (this is the magic fix!)
def navigate_to_booking(movie_id):
    st.session_state.selected_movie = movie_id
    st.session_state.current_menu = "Book Tickets"
    st.rerun()

if st.button("🎫 Book Now", key=f"book_{movie['movie_id']}", 
             on_click=navigate_to_booking, 
             args=(movie['movie_id'],),
             use_container_width=True):
    pass  # Callback handles everything

